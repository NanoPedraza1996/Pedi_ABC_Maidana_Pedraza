@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Error.</h1>
<h2 class="text-danger">Se ha producido un error al procesar la solicitud.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Pedir ID:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Modo de desarrollo</h3>
<p>
    Swapping to <strong>Desarrollo</strong> environment will display more detailed information about the error that occurred.
</p>
<p>
    <strong>El entorno de desarrollo no debe habilitarse para las aplicaciones implementadas.</strong>
    Puede resultar en mostrar información confidencial de excepciones a los usuarios finales.
    Para la depuración local, habilite el botón <strong>Desarrollo</strong> entorno estableciendo el <strong>ASPNETCORE_ENVIRONMENT</strong> variable de entorno a <strong>Desarrollo</strong>
    y reiniciar la aplicación.
</p>
